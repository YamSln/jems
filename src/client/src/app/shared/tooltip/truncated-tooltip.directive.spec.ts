import { TruncatedTooltipDirective } from './truncated-tooltip.directive';

describe('TruncatedTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new TruncatedTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
